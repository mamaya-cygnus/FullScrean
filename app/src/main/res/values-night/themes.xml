<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme compatible con AppCompat -->
    <style name="Theme.FullScrean" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryDark">@color/purple_700</item>
        <item name="colorAccent">@color/teal_200</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <!-- Fullscreen -->
        <item name="android:windowFullscreen">true</item>
    </style>

    <!-- Tema específico sin ActionBar -->
    <style name="Theme.FullScrean.NoActionBar" parent="Theme.FullScrean"/>
</resources>
